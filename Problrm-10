10) Merging the strings: Given two strings str1 and str2 in two files file1 and file2, write a program to create a third string str3 with one character from each array alternatively. If one string is longer than the other, append the extra characters to str3. Write str3 into a different text file called file3.

import java.io.*;
public class MergeStringsFromFiles {
    public static void main(String[] args) {
        try {
            BufferedReader br1 = new BufferedReader(new FileReader("file1.txt"));
            BufferedReader br2 = new BufferedReader(new FileReader("file2.txt"));
            String str1 = br1.readLine(); 
            String str2 = br2.readLine(); 
            br1.close();
            br2.close();
            StringBuilder str3 = new StringBuilder();
            int i = 0, j = 0;
            while (i < str1.length() && j < str2.length()) {
                str3.append(str1.charAt(i++));
                str3.append(str2.charAt(j++));
            }
            while (i < str1.length()) {
                str3.append(str1.charAt(i++));
            }
            while (j < str2.length()) {
                str3.append(str2.charAt(j++));
            }
            BufferedWriter bw = new BufferedWriter(new FileWriter("file3.txt"));
            bw.write(str3.toString());
            bw.close();
            System.out.println("Merged string written to file3.txt successfully!");
        } catch (IOException e) {
            System.out.println("An error occurred: " + e.getMessage());
        }
    }
}
